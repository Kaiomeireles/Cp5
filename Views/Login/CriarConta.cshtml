@model GameStoreMVC.Models.CriarContaViewModel

@{
    ViewData["Title"] = "Criar Conta";
}

<div class="container d-flex justify-content-center align-items-center" style="min-height: 70vh;">
    <div class="col-md-5 shadow-lg p-4 rounded-4" style="background-color: #ffffff; border: 1px solid var(--light-blue-bg);">

        <div class="text-center mb-4">
            <h3 class="fw-bold" style="color: var(--primary-navy);">Criar Conta</h3>
            <p class="text-muted small">Cadastre-se para começar a jogar</p>
        </div>

        <form asp-action="CriarConta" method="post">
            @Html.AntiForgeryToken()

            <div class="mb-3">
                <label asp-for="Nome" class="form-label small fw-semibold" style="color: var(--primary-navy);"></label>
                <input asp-for="Nome" class="form-control form-control-sm border-secondary-subtle"
                       placeholder="Seu nome completo" style="background-color: #f8f9fa;" />
                <span asp-validation-for="Nome" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Email" class="form-label small fw-semibold" style="color: var(--primary-navy);"></label>
                <input asp-for="Email" class="form-control form-control-sm border-secondary-subtle"
                       type="email" placeholder="nome@exemplo.com" style="background-color: #f8f9fa;" />
                <span asp-validation-for="Email" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Senha" class="form-label small fw-semibold" style="color: var(--primary-navy);"></label>
                <input asp-for="Senha" class="form-control form-control-sm border-secondary-subtle"
                       type="password" placeholder="••••••••" style="background-color: #f8f9fa;" />
                <span asp-validation-for="Senha" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="ConfirmarSenha" class="form-label small fw-semibold" style="color: var(--primary-navy);"></label>
                <input asp-for="ConfirmarSenha" class="form-control form-control-sm border-secondary-subtle"
                       type="password" placeholder="••••••••" style="background-color: #f8f9fa;" />
                <span asp-validation-for="ConfirmarSenha" class="text-danger small"></span>
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn text-white btn-sm fw-bold shadow-sm"
                        style="background-color: var(--accent-purple); border: none; padding: 10px;">
                    Criar Conta
                </button>
            </div>

            <hr class="opacity-25 mt-4" style="color: var(--primary-navy);">

            <div class="text-center mt-3">
                <p class="text-muted small mb-1">Já tem uma conta?</p>
                <a asp-action="Login" asp-controller="Login" class="text-decoration-none small fw-bold"
                   style="color: var(--accent-purple);">
                    Fazer login
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

